# 如何重现每一节代码

## 为什么不复制多个 Maven 项目

如果在当前仓库中复制多份完整项目，每份都包含一个 `pom.xml`，IDEA 可能把它们识别成多个 Maven 项目。这样会增加目录、依赖和运行配置的复杂度，不适合零基础学习。

本教程使用 Git 保存每一节的完整代码快照。每完成一节并验证通过后，会同时创建：

1. 一个 Git commit，记录这一节修改了什么。
2. 一个 Git tag，为完成状态取一个容易识别的名字。

Git tag 可以理解为贴在某个 commit 上的固定书签。后面的代码继续变化时，tag 指向的旧代码不会变化。

## 已保存的课程版本

| Tag | 对应文档 | 对应内容 |
| --- | --- | --- |
| `lesson-01-hello` | 第 3 篇 | Spring Boot 初始化和 `/hello` 接口 |
| `lesson-02-book-json` | 第 6 篇 | 创建 `Book` 对象并返回 JSON |
| `lesson-03-book-list` | 第 7 篇 | 使用 `List` 查询全部图书和按编号查询 |
| `lesson-04-book-create` | 第 8 篇 | 使用 POST 和 JSON 新增图书 |
| `lesson-05-book-update` | 第 9 篇 | 使用 PUT 修改指定编号的图书 |
| `lesson-06-book-delete` | 第 10 篇 | 使用 DELETE 删除指定编号的图书 |
| `lesson-07-layered-architecture` | 第 11 篇 | Controller、Service、Repository 分层 |
| `lesson-08-h2-jpa` | 第 12 篇 | 使用 H2 和 Spring Data JPA 持久化图书数据 |
| `lesson-09-validation-exception` | 第 13 篇 | 参数校验和统一异常处理 |
| `lesson-10-tests` | 第 14 篇 | 三层自动化测试 |

后续每完成一节，表格会继续增加。

### 为什么 tag 编号和文档编号不一样

tag 只在代码发生变化的节建立。第 1、2、4、5 篇分别讲检查环境、导入项目、认识项目结构和理解 Maven，这几篇不修改代码，所以没有对应 tag。因此 `lesson-08-h2-jpa` 对应的是第 12 篇，不是第 8 篇。查找 tag 时以上面表格的“对应文档”一列为准。

### tag 里的文档可能不是最新版本

tag 记录的是这一节验证通过时的代码和文档。如果之后发现某篇文档讲得不够清楚并做了修订，修订会提交到 `main` 分支。需要最新文档时，直接看仓库 `docs/` 目录，不要以导出的 ZIP 为准。代码部分不受影响，tag 指向的代码始终是那一节完成时的状态。

## 1. 查看所有课程版本

在 IDEA 底部打开 Terminal，执行：

```bash
git tag --list "lesson-*"
```

预期能够看到：

```text
lesson-01-hello
lesson-02-book-json
lesson-03-book-list
lesson-04-book-create
lesson-05-book-update
lesson-06-book-delete
lesson-07-layered-architecture
lesson-08-h2-jpa
lesson-09-validation-exception
lesson-10-tests
```

## 2. 推荐方式：导出一份独立代码副本

这种方式不会改变当前正在学习的代码，最适合初学者。

先在项目根目录创建导出目录：

```bash
mkdir -p exports
```

导出第一节：

```bash
git archive --format=zip --output=exports/lesson-01-hello.zip lesson-01-hello
```

导出第二节：

```bash
git archive --format=zip --output=exports/lesson-02-book-json.zip lesson-02-book-json
```

执行后，`exports` 目录中会出现对应的 ZIP 文件。ZIP 中包含该课程版本当时的完整源码和文档。

`exports/` 已加入 `.gitignore`，导出的压缩包不会被提交到 Git 仓库。

## 3. 解压并用 IDEA 单独打开

以第一节为例：

1. 在 Finder 中找到 `exports/lesson-01-hello.zip`。
2. 双击解压。
3. 建议把解压后的目录重命名为 `lesson-01-hello`。
4. 在 IDEA 中选择 **File → Open**。
5. 选择解压后的目录。
6. 如果 IDEA 询问打开方式，选择 **New Window**。
7. 等待 Maven 导入完成。
8. 确认 Project SDK 和 Maven Runner 都使用 Java 8。
9. 运行 `BookApiApplication`。

这样可以同时保留最新项目和旧章节副本，互不影响。

## 4. 查看某一节修改了什么

查看第一节提交内容：

```bash
git show lesson-01-hello
```

查看第二节提交内容：

```bash
git show lesson-02-book-json
```

命令会显示提交信息和代码差异。按 `q` 可以退出查看界面。

只查看文件列表，可以执行：

```bash
git show --stat lesson-02-book-json
```

## 5. 不推荐初学者直接切换当前目录

Git 也支持把当前工作目录切换到旧 tag：

```bash
git switch --detach lesson-01-hello
```

但这会改变当前目录中的文件，而且在有未提交修改时可能无法切换。初学阶段优先使用 ZIP 导出方式。

如果已经切换到旧 tag，可以通过下面的命令回到最新学习代码：

```bash
git switch main
```

## 每节完成后的保存流程

```text
完成本节代码和文档
        ↓
运行自动测试
        ↓
在浏览器或接口工具中人工验证
        ↓
创建 Git commit
        ↓
创建 lesson 标签
        ↓
推送 commit 和 lesson 标签
        ↓
进入下一节
```

只有验证通过的代码才会建立课程标签。
